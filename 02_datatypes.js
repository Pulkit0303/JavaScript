"use strict"; //Used to indicate the newer version of JS is used
//By default it is used

//alert(3+3) //We should not be using this because we are not using browser to run our code.


//Never Ever type these kind of code in your application

/*console.log(3+
    3);
    console.log("hi"); console.log("bye")
*/

let name = "Pulkit"
let age = 22
let isLogged = false

/*
1. number => 2 power 53(Max)
2. bigint       //For large values (Facebook, Stocks)
3. string => " "
4. boolean => true/false
5. null         //This is a standalone value
6. undefined 

*/

console.table([typeof age, typeof "Pulkit", typeof undefined]);

console.log(typeof null);   //Very Much Impt:   null is a type of an object